
add_library(utils SHARED log.h error.h bitmap_utils.h bitmap_utils.cpp)

find_library(log-lib log)

#include_directories(${PROJECT_SOURCE_DIR})

target_link_libraries(utils
        ${log-lib}
        jnigraphics
        android)